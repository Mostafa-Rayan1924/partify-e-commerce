import RegisterForm from "./RegisterForm";

const page = () => {
  return (
    <section className="mt-[80px] py-10  grid place-items-center">
      <RegisterForm />
    </section>
  );
};

export default page;
