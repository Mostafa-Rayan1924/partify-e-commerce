import LoginForm from "./LoginForm";

const page = () => {
  return (
    <section className="mt-[120px] py-10  grid place-items-center">
      <LoginForm />
    </section>
  );
};

export default page;
